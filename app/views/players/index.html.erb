<h1>There are <%= @players.count %> players in our Team</h1>

<table>
<thead>
    <tr>
        <td>First Name</td>
        <td>Last Name</td>
    </tr>
</thead>

<tbody>
<% @players.each do |player| %>
    <tr>
        <td> <%= player.fisrt_name %> </td>
        <td><%= player.last_name %></td>
    </tr>
<% end %>
</tbody>
</table>
<a href="/players/new">Add New Player</a>